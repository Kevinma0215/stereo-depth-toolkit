[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "stereo-depth-toolkit"
version = "0.1.0"
requires-python = ">=3.10,<3.11"
dependencies = ["numpy", "PyYAML", "opencv-python"]

[project.scripts]
stereo-depth = "stereo_depth.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
