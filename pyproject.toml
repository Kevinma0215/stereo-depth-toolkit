[project]
name = "stereo-depth-toolkit"
version = "0.1.0"
dependencies = [
  "numpy",
  "opencv-python",
  "typer[all]",
  "pyyaml",
]

[project.scripts]
stereo-depth = "stereo_depth.cli.app:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
